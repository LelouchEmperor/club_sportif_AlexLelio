<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier Catégorie</title>
</head>
<body>
    <h1>Modifier Catégorie</h1>

    <form action="{{ path('modifier_categorie', {'id': categorie.id}) }}" method="post">
        <label for="nom">Nom:</label>
        <input type="text" id="nom" name="nom" value="{{ categorie.nom }}" required>

        <label for="code_raccourci">Code Raccourci:</label>
        <input type="text" id="code_raccourci" name="code_raccourci" value="{{ categorie.codeRaccourci }}" required>

        <!-- Ajoutez un champ masqué pour l'ID -->
        <input type="hidden" name="id" value="{{ categorie.id }}">

        <button type="submit">Sauvegarder</button>
        <a href="{{ path('liste_categories') }}">Annuler</a>
    </form>
</body>
</html>
