<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Licenciés</title>
</head>
<body>
    <h1>Liste des Licenciés</h1>

    <ul>
        {% for licencie in licencies %}
            <li>
                {{ licencie.getNom() }} {{ licencie.getPrenom() }} (Numéro de Licence: {{ licencie.getNumeroLicence() }})
                <!-- Ajoutez d'autres informations si nécessaire, par exemple, pour afficher le contact associé ou la catégorie -->

                <a href="{{ path('modifier_licencie', {'id': licencie.getId()}) }}">Modifier</a>
                <a href="{{ path('supprimer_licencie', {'id': licencie.getId()}) }}" onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce licencié ?')">Supprimer</a>
            </li>
        {% endfor %}
    </ul>

    <a href="{{ path('creer_licencie') }}">Créer un Licencié</a>
</body>
</html>
