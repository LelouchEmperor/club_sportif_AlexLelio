<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Éducateurs</title>
</head>
<body>
    <h1>Liste des Éducateurs</h1>

    <ul>
        {% for educateur in educateurs %}
            <li>
                {{ educateur.getNom() }} {{ educateur.getPrenom() }} ({{ educateur.getEmail() }} - {{ educateur.getNumeroTel() }})
                {% if educateur.getIsAdmin() %}
                    (Administrateur)
                {% endif %}
                <a href="{{ path('modifier_educateur', {'id': educateur.getId()}) }}">Modifier</a>
                <a href="{{ path('supprimer_educateur', {'id': educateur.getId()}) }}" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet éducateur ?')">Supprimer</a>
            </li>
        {% endfor %}
    </ul>

    <a href="{{ path('creer_educateur') }}">Créer un Éducateur</a>
</body>
</html>
